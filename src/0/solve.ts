type InputArray = Array<string>;

export function solve(input: InputArray) {
  const limit = 20;
  const sampleInput = input.slice(0, limit);
  const realInput = input;

  let result = '';

  // Your code here

  return result;
}
